angular.module('feedbackService', [])

    .factory('Feedback', function($http)
    {

        var feedbackFactory = {};

        feedbackFactory.create = function(feedbackData) 
	{
            return $http.get('/api/feedback', feedbackData);
        };

        return feedbackFactory;

    });
